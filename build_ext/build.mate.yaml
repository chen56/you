# Read about `build.mate.yaml` at https://pub.dev/packages/build_config
targets:
  $default:
#    auto_apply_builders: false
    sources:
      - test/abc/y.dart
      - $package$
      - lib/$lib$
#      - $package$
#      - lib/$lib$
#      - $lib$
    builders:
      mateBuilder:
        generate_for:
          - test/abc/y.dart
        enabled: true
