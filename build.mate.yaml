# Read about `build.mate.yaml` at https://pub.dev/packages/build_config
targets:
  $default:
    builders:
      flutter_note:mateBuilder:
        generate_for:
          - /Users/cccc/git/chen56/flutter-note/lib/**/*.dart

